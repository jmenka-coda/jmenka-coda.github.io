# 🎨 Paper.js Онлайн Рисовалка

Современная интерактивная рисовалка с совместным рисованием в реальном времени в отдельных комнатах.

## ✨ Возможности

- **Комнатная система** - каждая комната имеет независимый холст
- **Совместное рисование** - рисуйте вместе с другими пользователями в реальном времени
- **Инструменты рисования** - карандаш, ластик, настройка размера кисти
- **Цветовая палитра** - разные цвета для левой и правой кнопки мыши
- **Адаптивный дизайн** - работает на всех устройствах
- **Чистая архитектура** - модульная организация кода

## 🚀 Быстрый запуск

### Требования
- Node.js 14+
- npm или yarn

### Установка
```bash
# Клонирование репозитория
git clone <repository-url>
cd canvas-drawing-app

# Установка зависимостей
npm install
```

### Запуск

**Режим разработки:**
```bash
npm run dev
```

**Продакшн:**
```bash
npm start
```

**С отладкой:**
```bash
npm run debug
```

Сервер запустится на порту 4000 (или на порту из переменной окружения PORT).

> **Примечание для Windows:** Проект использует `cross-env` для кроссплатформенной совместимости переменных окружения.

## 📁 Структура проекта

```
canvas-drawing-app/
├── config/                 # Конфигурационные файлы
│   └── default.json       # Настройки по умолчанию
├── src/                   # Исходный код сервера
│   ├── middleware/        # Middleware функции
│   │   └── errorHandler.js
│   ├── rooms/            # Логика управления комнатами
│   │   └── roomManager.js
│   ├── server/           # Серверные компоненты
│   │   ├── app.js        # Express приложение
│   │   └── server.js     # Основной сервер
│   ├── socket/           # Обработка Socket.IO
│   │   └── socketHandler.js
│   └── utils/            # Утилиты
│       ├── config.js     # Менеджер конфигурации
│       ├── logger.js     # Логгер
│       └── validation.js # Валидация данных
├── routes/               # Маршруты API
│   ├── api.js           # API маршруты
│   └── pages.js         # Маршруты страниц
├── views/               # Шаблоны страниц
│   ├── drawing.html     # Страница рисования
│   └── rooms.html       # Страница выбора комнат
├── public/              # Статические файлы
│   ├── css/
│   │   ├── components/  # CSS компоненты
│   │   │   ├── buttons.css
│   │   │   ├── cards.css
│   │   │   └── forms.css
│   │   ├── main.css     # Основные стили
│   │   └── rooms.css    # Стили страницы комнат
│   └── js/
│       ├── pages/       # Логика страниц
│       │   ├── drawing-page.js
│       │   └── rooms-page.js
│       ├── services/    # Сервисы
│       │   └── socket-service.js
│       ├── tools/       # Инструменты рисования
│       │   ├── eraser-tool.js
│       │   ├── hand-tool.js
│       │   └── pencil-tool.js
│       ├── canvas-transform.js
│       ├── canvas.js
│       ├── socket.js
│       └── tool-manager.js
├── index.html           # Точка входа (перенаправление)
├── index.js            # Главный файл приложения
├── package.json         # Зависимости и скрипты
└── README.md           # Документация
```

## 🌐 Доступ

После запуска сервер будет доступен по адресам:

- **Главная страница:** `http://localhost:4000` (перенаправляет на комнаты)
- **Комнаты:** `http://localhost:4000/rooms`
- **Рисование:** `http://localhost:4000/drawing?room=room-name`

## 🛠 API Endpoints

### Страницы
- `GET /` - Перенаправление на комнаты
- `GET /rooms` - Страница выбора комнат
- `GET /drawing` - Страница рисования

### API
- `GET /api/rooms` - Список активных комнат
- `GET /api/rooms/:roomName` - Информация о комнате
- `GET /api/stats` - Статистика сервера

## 🔧 Настройка

### Конфигурация
Настройки находятся в `config/default.json`:

```json
{
  "server": {
    "port": 4000,
    "host": "localhost",
    "env": "development"
  },
  "socket": {
    "cors": {
      "origin": "*",
      "methods": ["GET", "POST"]
    }
  },
  "rooms": {
    "maxNameLength": 50,
    "minNameLength": 2,
    "cleanupInterval": 300000
  },
  "drawing": {
    "maxStrokePoints": 1000,
    "maxStrokesPerRoom": 5000
  }
}
```

### Переменные окружения
- `PORT` - порт сервера (по умолчанию 4000)
- `HOST` - хост сервера (по умолчанию localhost)
- `NODE_ENV` - окружение (development/production)

## 🎨 Использование

1. **Запустите сервер**
2. **Откройте** `http://localhost:4000`
3. **Создайте комнату** или присоединитесь к существующей
4. **Начните рисовать** - изменения видны всем в комнате в реальном времени

### Инструменты рисования
- **Карандаш** - рисование линиями
- **Ластик** - стирание
- **Рука** - перемещение холста
- **Размер кисти** - ползунок для настройки толщины линий
- **Цвета** - отдельные цвета для ЛКМ и ПКМ

## 🏗 Архитектура

### Серверная часть
- **Express.js** - веб-фреймворк
- **Socket.IO** - реальное время
- **Модульная архитектура** - разделение ответственности

### Клиентская часть
- **Vanilla JavaScript** - без фреймворков
- **Paper.js** - работа с Canvas API
- **Компонентная структура** - переиспользуемые компоненты

### Коммуникация
- **WebSocket** через Socket.IO
- **JSON** для обмена данными
- **Событийная модель** для обновлений в реальном времени

## 📜 Скрипты

```bash
# Запуск в продакшне
npm start

# Режим разработки с автоперезагрузкой
npm run dev

# Режим отладки
npm run debug

# Линтинг кода
npm run lint

# Форматирование кода
npm run format

# Очистка и переустановка зависимостей
npm run clean
```

## 🤝 Разработка

### Добавление нового инструмента рисования
1. Создайте файл в `public/js/tools/`
2. Реализуйте функции `initToolName()` и `activateToolName()`
3. Добавьте инструмент в `tool-manager.js`
4. Обновите HTML интерфейс

### Добавление новой страницы
1. Создайте HTML файл в `views/`
2. Добавьте маршрут в `routes/pages.js`
3. Создайте JavaScript логику в `public/js/pages/`

### Добавление API эндпоинта
1. Добавьте маршрут в `routes/api.js`
2. Реализуйте логику в соответствующем модуле

## 🔧 Технологии

- **Backend:** Node.js, Express.js, Socket.IO
- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Canvas:** Paper.js
- **Real-time:** Socket.IO
- **Build:** npm scripts, cross-env (кроссплатформенность)
- **Linting:** ESLint
- **Formatting:** Prettier

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера и сервера на ошибки
2. Убедитесь, что порт 4000 свободен
3. Проверьте версию Node.js (минимум 14)
4. Попробуйте очистить кэш браузера